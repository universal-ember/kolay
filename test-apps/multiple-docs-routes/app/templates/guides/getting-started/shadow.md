# Home's own getting-started

This page exists to collide with the scoped `guides` mount. The co-located
group's pages live at the root, so this sits at `/guides/getting-started` —
the same manifest path the `guides` group occupies. Visiting the mount at
`/help/getting-started` must resolve inside `guides`, not land here.
